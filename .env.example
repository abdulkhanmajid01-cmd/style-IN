# Yeh file safe hai commit karne ke liye — isme sirf variable NAMES hain,
# real values nahi. Jab Phase 2/3 (backend/database) banega, tab is file ko
# copy karke ".env.local" banayenge aur real values daalenge — woh file
# .gitignore mein hai, kabhi GitHub par nahi jayegi.

# Phase 3 — Database
# DATABASE_URL="postgresql://user:password@host:port/dbname"

# Phase 2 — Admin auth
# ADMIN_PASSWORD_HASH=""
# SESSION_SECRET=""
